#include <iostream>
using namespace std;

int main() {
    int increment = 0;
    int m = 10;
    int n = 20;
    
    for(int i = 1; i <= m; i++) {
	for(int j = 1; j <= n; j++) {
	    if(i != j) {
		increment++;
	    }
	}
    }

    cout << "Incremented " << increment << " times." << endl;
    
    return 0;
}
